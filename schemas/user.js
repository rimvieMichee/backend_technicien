//user swagger

/**
 * @swagger
 * components:
 *   schemas:
 *     User:
 *       type: object
 *       required:
 *         - firstName
 *         - lastName
 *         - email
 *         - password
 *         - departement
 *         - post
 *         - phone
 *         - role
 *       properties:
 *         firstName:
 *           type: string
 *         lastName:
 *           type: string
 *         email:
 *           type: string
 *         password:
 *           type: string
 *         departement:
 *           type: string
 *         post:
 *           type: string
 *         phone:
 *           type: string
 *         Avatar:
 *           type: string
 *         role:
 *           type: string
 */


// module.exports